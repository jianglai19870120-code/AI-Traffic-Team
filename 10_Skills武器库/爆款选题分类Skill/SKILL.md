---
name: 爆款选题分类Skill
description: 从对标账号库读取账号级 Excel 表，把 `视频信息` 列提取为爆款选题，并按科学创业、能力成长、赚钱财富、个人IP、AI科技、其他类型写入爆款选题库；用于对标账号内容批量归类、分类表更新和审核报告生成，不写 `00_手动输入选题表.md`。
---

# 爆款选题分类Skill

## 作用定位

本 Skill 负责把 `02_资产中心/03_对标账号库` 中的博主对标表，分类沉淀到 `02_资产中心/04_爆款选题库`。

只做三件事：

1. 从账号表 `视频信息` 列提取选题。
2. 按主题领域分类。
3. 写入 6 张分类选题表并生成审核报告。

禁止：

- 不改原始账号表。
- 不写 `00_手动输入选题表.md`。
- 不生成文案。
- 不拆解爆款开头。
- 不读取 `文案` 列做选题提取、总结或分类。

## 默认路径

- 输入目录：`02_资产中心/03_对标账号库`
- 输出目录：`02_资产中心/04_爆款选题库`
- 审核目录：`03_工作流中心/01_短视频主工作流/99_运行记录`

外部用户模式：

- 用 `--input-dir`、`--output-dir`、`--audit-dir` 显式传入自己的目录。

## 输入字段

必需字段：

- `视频信息`

建议字段：

- `点赞数`
- `链接`
- `发布时间`

字段规则：

- `视频信息` 是唯一正式选题来源。
- 选题和分类都只根据 `视频信息` 清洗后的内容判断。
- `链接` 用于去重；没有链接时按 `博主名 + 选题` 去重。
- `视频信息` 为空时，该行写入审核报告，不进入分类表。

## 输出分类表

只写以下 6 张表：

- `01_科学创业选题表.md`
- `02_能力成长选题表.md`
- `03_赚钱财富选题表.md`
- `04_个人IP选题表.md`
- `05_AI科技选题表.md`
- `99_其他类型选题表.md`

不写：

- `00_手动输入选题表.md`

每张表字段固定为：

| 选题 | 主题分类 | 博主名 | 点赞数 | 链接 | 发布时间 | 是否选用 |
|---|---|---|---:|---|---|---|

`是否选用` 默认留空，供人工填写。

## 选题清洗规则

从 `视频信息` 提取选题时：

- 只保留第一个 `#` 之前的内容，`#` 后全部视为话题标签丢弃。
- 压缩换行和多余空格。
- 去掉明显平台噪音和重复空白。
- 清洗后 30 字以内，直接作为选题。
- 清洗后超过 30 字，优先取第一句；第一句仍过长时，取逗号、分号、顿号前的短句；仍过长时截取前 30 字，并写入审核报告。
- 清洗后为空则跳过并写入审核报告。

## 分类规则

分类不做简单关键词命中。先判断这条选题的“主要承诺”是什么，再落分类。

### 科学创业

创业、商业模式、公司经营、产品、组织、增长、战略、融资、企业服务、创新商业化。

边界：

- 如果讲的是个人怎么赚钱，优先归 `赚钱财富`。
- 如果讲的是做账号获客，优先归 `个人IP`。

### 能力成长

学习、认知、效率、行动力、决策、表达、习惯、复盘、自我管理、职业能力。

边界：

- 如果核心承诺是变现或赚多少钱，归 `赚钱财富`。

### 赚钱财富

赚钱、副业、收入、财富、现金流、职业变现、商业机会、普通人赚钱路径、投资理财。

边界：

- 如果核心方法是做内容账号、涨粉、私域获客，归 `个人IP`。

### 个人IP

自媒体、短视频、内容创作、账号定位、涨粉、流量、私域、个人品牌、信任、成交、IP 变现。

边界：

- 如果只是讲 AI 工具做内容，且重点是工具能力，归 `AI科技`。
- 如果重点是账号增长、内容获客或个人品牌，归 `个人IP`。

### AI科技

AI 工具、模型、Agent、编程、自动化、科技产品、AI 应用、技术趋势、平台发布。

边界：

- 如果 AI 只是赚钱方法里的工具，且重点是赚钱路径，归 `赚钱财富`。

### 其他类型

信息不足、多类冲突、无法判断主要承诺，或不属于当前五类。

## 冲突处理

当多个分类同时成立时：

1. 先看 `视频信息` 的核心承诺。
2. 判断哪个分类最能回答“观众为什么要点开/继续看”。
3. 仍冲突时写入 `99_其他类型`，原因只写入审核报告，不写入分类表。

## 读取老式 Excel

当前账号库里可能存在老式 Excel 文件伪装成 `.xlsx`。

脚本必须：

- 读取标准 xlsx。
- 检测不可读或老式 Excel 文件。
- 把不可读文件写入审核报告。
- 不允许因为单个文件不可读中断整批。

默认不自动安装额外依赖，也不自动转换老式 Excel。

## 执行方式

优先运行：

`python scripts/classify_benchmark_topics.py`

只跑单个博主：

`python scripts/classify_benchmark_topics.py --blogger 样板博主`

外部目录：

`python scripts/classify_benchmark_topics.py --input-dir <账号表目录> --output-dir <选题库目录> --audit-dir <审核目录>`

## 验收标准

- `00_手动输入选题表.md` 不被修改。
- 每条选题只进入一张分类表。
- 重复运行不重复追加同一链接。
- 不可读账号表进入审核报告。
- 分类表只包含轻量字段，不写完整文案。

---

• 带你用AI，把你的能力变成你的生意
• 有使用问题，或加入我的会员答疑群！
• 姜来已来2046，联系微信： lact175

---
