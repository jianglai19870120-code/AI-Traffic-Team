请使用 `干货型文案生成Skill`。

输入：

选题：[可选；未填写时读取爆款选题库 00_手动输入选题表.md 中第一条干货型待生成选题]

要求：

1. 不生成开头，不读取爆款开头库，只生成正文方案。
2. 根据 `文案结构` 路由；当 `文案结构=干货型` 时，使用本 Skill。
3. 如果用户显式指定选题，即使该选题已生成，也重新生成一个新时间戳文件。
4. 进入模块库之前，先围绕选题判断核心矛盾、用户卡点和结果目标。
5. 只输出一条主线，主线先定，再反向匹配误区和步骤模块。
6. 模块调用优先读取 `99_工作纪实模块` 下的金句、误区、步骤原子模块；明显不匹配时跳过。
7. 工作纪实不足时，再使用书籍拆解的误区、步骤、金句模块补足结构。
8. 两个误区点可以来自不同书、不同工作纪实、不同误区模块。
9. 第一组 `步骤1/2/3` 来自一个完整三步步骤模块，共同解决一个具体问题。
10. 第二组 `步骤1/2/3` 来自另一个完整三步步骤模块，共同解决另一个具体问题。
11. 两组步骤模块可以来自不同书、不同工作纪实、不同模块。
12. 第一组问题和第二组问题之间必须有递进关系，整体逻辑和每组内部子逻辑都要成立。
13. 输出文件名固定为 `选题_YYYY-MM-DD_HH-mm-ss.md`。
14. 文件内 H1 标题必须与文件名主体一致。
15. 标题下方固定输出 `文案结构：干货型`。
16. 只输出一张四列表，字段固定为：
    - `序号`
    - `逻辑关系`
    - `选用模块`
    - `对应来源`
17. 表格固定 11 行：
    - `误区1`
    - `误区2`
    - `过渡句`
    - `步骤1`
    - `步骤2`
    - `步骤3`
    - `过渡句`
    - `步骤1`
    - `步骤2`
    - `步骤3`
    - `金句收口`
18. 第 3 行过渡句从两个误区转入解决路径；第 7 行过渡句从第一个问题推进到第二个问题。
19. 过渡句不调用模块，来源留空，只写一句口播式承接。
20. 步骤模块优先选择正好 3 步的模块；不能截断步骤，不能补写不存在的步骤。
21. `对应来源` 列必须使用 Obsidian 直接链接。
22. 最终保存到 `02_资产中心/06_生成正文库/01_干货型文案`。
23. 新文件生成后，必须立刻调用 `E:\AI流量工厂\01_Agent系统\02_小审-质量审核Agent\scripts\audit_machine_drafts.py --file <新生成文件绝对路径> --write-report`。
24. 只有审核通过，才允许回写 `生成时间` 和 `生成状态=已生成`。
25. 如果审核退回，则回写 `生成时间` 和 `生成状态=退回修改`，并明确说明退回原因。

禁止：

- 输出旧三方案标题
- 输出 `方案 1/2/3`
- 输出旧润色列
- 输出旧 4 行结构
- 输出开头复刻
- 读取 `02_资产中心/05_爆款开头库`
- 跳过机器文案放行审核直接把状态写成 `已生成`
