interface:
  display_name: "热门播客爆款文案"
  short_description: "BK爆款开头卡片 + B播客内容模块生成整篇口播稿"
  default_prompt: "Use $remen-boke-baokuan-wenan-skill with five explicit inputs: a B podcast transcript, a BK opening card, the matching breakdown directory, a viewpoint count, and an output directory. Generate one opening-reproduction table and three mainline proposal tables, then validate the saved Markdown file."
