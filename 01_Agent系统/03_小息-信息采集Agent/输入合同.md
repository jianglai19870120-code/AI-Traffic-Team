# 小息-信息采集Agent 输入合同

上游来源：

你、小姜

标准输入：

- 书籍原始文件
- 得到大脑工作纪实同步任务

输入要求：

- 输入必须先有明确来源
- 书类资料默认已经直接放进原始知识库
- 非 `md` 资料进入后，要先走格式标准化入库
- 扫描型 PDF 必须先做 OCR，再生成正式 `md`
- 得到大脑同步只读取环境变量 `DEDAO_BRAIN_CLIENT_ID` 和 `DEDAO_BRAIN_API_KEY`，不得读取或写入明文密钥
- 如果输入不完整，先回到小姜，不直接猜

---
• 带你用AI，把你的能力变成你的生意
• 有使用问题，或加入我的会员答疑群！
• 姜来已来2046，联系微信： lact175
---
