# 总控中心 调用规则

规则：

这里只放系统级总规则，不放单个 Agent 的业务细节。小姜 是唯一直接和你对话的 Agent。

默认工作方式：

- 先看输入是否完整。
- 再判断是否属于本岗位职责。
- 所有 Agent 调用 Skill 前，必须遵守 `全局Skill调用流程.md`。
- 所有 Agent 都必须优先检查 `10_Skills武器库`，不能只看 Codex 已安装目录。
- 属于本岗位才输出正式资产。
- 不属于本岗位的内容统一退回小姜。
